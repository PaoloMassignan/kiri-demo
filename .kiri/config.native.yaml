oauth_passthrough: true          # set to false if using API key mode
similarity_threshold: 0.75
hard_block_threshold: 0.90
action: sanitize
proxy_port: 8765
embedding_model: all-MiniLM-L6-v2
# L3 classifier uses Ollama if available; otherwise fails-open (L1+L2 always active).
# Do not edit this file — it is a template. start.sh writes config.native.local at runtime.
