oauth_passthrough: true          # set to false if using API key mode
similarity_threshold: 0.75
hard_block_threshold: 0.90
action: sanitize
proxy_port: 8765
embedding_model: all-MiniLM-L6-v2
# llm_backend and llm_model_path are written at runtime by start-native.sh
# into .kiri/config.native.local (gitignored). Do not edit this file.
